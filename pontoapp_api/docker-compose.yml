version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: pontoapp-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pontoapp_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - pontoapp-network
#
#  # Opcional: PgAdmin para gerenciar o banco visualmente
#  pgadmin:
#    image: dpage/pgadmin4:latest
#    container_name: pontoapp-pgadmin
#    restart: unless-stopped
#    environment:
#      PGADMIN_DEFAULT_EMAIL: admin@pontoapp.com
#      PGADMIN_DEFAULT_PASSWORD: admin
#      PGADMIN_LISTEN_PORT: 80
#    ports:
#      - "5050:80"
#    volumes:
#      - pgadmin_data:/var/lib/pgadmin
#    depends_on:
#      - postgres
#    networks:
#      - pontoapp-network

volumes:
  postgres_data:
    driver: local
  pgadmin_data:
    driver: local

networks:
  pontoapp-network:
    driver: bridge